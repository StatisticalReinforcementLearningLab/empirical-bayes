\usepackage{titlesec}
\usepackage[many]{tcolorbox}

% Adjust spacing after the chapter title
\titlespacing*{\chapter}{0cm}{-2.0cm}{0.50cm}
\titlespacing*{\section}{0cm}{0.50cm}{0.25cm}

% Indent 
\setlength{\parindent}{0pt}
\setlength{\parskip}{1ex}

% --- Theorems, lemma, corollary, postulate, definition ---
% \numberwithin{equation}{section}

\newtcolorbox{problem}{%
    enhanced,
    colback = gray!7,  % Background color
    colbacktitle = black!5,
    coltitle = black,
    boxrule = 0pt,       % No border frame
    frame hidden,        % Hide default frame
    borderline west = {0.5mm}{0.0mm}{black},  % Black line on the left
    breakable,           % Allow the box to break across pages
    before skip = 1ex,   % Space before the box
    after skip = 3ex     % Space after the box
}

\tcbuselibrary{skins, breakable}

% --- You can define your own color box. Just copy the previous \newtcbtheorm definition and use the colors of yout liking and the title you want to use.